import { solvePartOne, solvePartTwo } from './solver';

describe('Day X', () => {
  let input = [] as string[];

  beforeEach(() => {
    input = [];
  });
  test('solve part one', () => {});
  test('solve part two', () => {});
});
